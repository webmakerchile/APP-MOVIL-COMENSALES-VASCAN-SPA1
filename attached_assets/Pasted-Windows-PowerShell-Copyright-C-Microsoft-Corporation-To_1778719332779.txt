Windows PowerShell
Copyright (C) Microsoft Corporation. Todos los derechos reservados.

Prueba la nueva tecnología PowerShell multiplataforma https://aka.ms/pscore6

PS C:\Windows\system32> $ErrorActionPreference = "Stop"
>> $Install = "C:\BuenaMezcla"
>> $Cloud   = "https://vascan.replit.app"
>> $Token   = "g7Mqlz9HyQdlZqunnXcMCD6Qgv9PLGdc"
>> $Nombre  = "Totem-$($env:COMPUTERNAME)"
>>
>> # 0) Matar node viejo
>> Get-CimInstance Win32_Process -Filter "Name='node.exe'" -ErrorAction SilentlyContinue |
>>   Where-Object { $_.ExecutablePath -like "$Install\*" } |
>>   ForEach-Object { Stop-Process -Id $_.ProcessId -Force -ErrorAction SilentlyContinue }
>> Start-Sleep -Seconds 2
>>
>> # 1) Buscar better-sqlite3
>> Write-Host "=== 1) Buscando better-sqlite3 ===" -ForegroundColor Yellow
>> $bs3Dir = $null
>> $candidates = @(
>>   "$Install\node_modules\better-sqlite3",
>>   "$Install\totem\node_modules\better-sqlite3"
>> )
>> foreach ($c in $candidates) {
>>   if (Test-Path "$c\package.json") { $bs3Dir = $c; break }
>> }
>> if (-not $bs3Dir) {
>>   $found = Get-ChildItem -Path $Install -Filter "better_sqlite3.node" -Recurse -ErrorAction SilentlyContinue | Select-Object -First 1
>>   if ($found) {
>>     $bs3Dir = (Get-Item $found.Directory.Parent.Parent.FullName).FullName
>>     Write-Host "Encontrado en $bs3Dir" -ForegroundColor Green
>>   }
>> }
>> if (-not $bs3Dir) {
>>   Write-Host "No esta instalado. Instalando con npm..." -ForegroundColor Yellow
>>   Push-Location $Install
>>   & "$Install\node\npm.cmd" install better-sqlite3@12.9.0 --omit=dev --ignore-scripts --no-audit --no-fund 2>&1 | Out-Host
>>   Pop-Location
>>   $bs3Dir = "$Install\node_modules\better-sqlite3"
>>   if (-not (Test-Path "$bs3Dir\package.json")) { Write-Host "FALLO al instalar better-sqlite3" -ForegroundColor Red; Read-Host; exit 1 }
>>   # Bajar el .node nativo
>>   $rel = "$bs3Dir\build\Release"
>>   New-Item -ItemType Directory -Force -Path $rel | Out-Null
>>   if (-not (Test-Path "$rel\better_sqlite3.node")) {
>>     Invoke-WebRequest -Uri "$Cloud/totem/bs3-win.node" -OutFile "$rel\better_sqlite3.node" -UseBasicParsing
>>   }
>>   Write-Host "better-sqlite3 instalado en $bs3Dir" -ForegroundColor Green
>> }
>>
>> # 2) IP local
>> $ipLocal = ""
>> try {
>>   $ipLocal = (Get-NetIPAddress -AddressFamily IPv4 -ErrorAction SilentlyContinue |
>>     Where-Object { $_.IPAddress -notlike "127.*" -and $_.IPAddress -notlike "169.*" } |
>>     Select-Object -First 1).IPAddress
>> } catch {}
>>
>> # 3) Registrar contra la nube
>> Write-Host "=== 2) Registrando contra la nube ===" -ForegroundColor Yellow
>> $body = @{ nombre = $Nombre; hostname = $env:COMPUTERNAME; ipLocal = $ipLocal; version = "1.0.0" } | ConvertTo-Json -Compress
>> try {
>>   $resp = Invoke-RestMethod -Uri "$Cloud/api/totem/register" -Method POST `
>>             -Headers @{ "x-bootstrap-token" = $Token; "content-type" = "application/json" } `
>>             -Body $body -TimeoutSec 15
>> } catch {
>>   Write-Host "Registro FALLO: $_" -ForegroundColor Red; Read-Host; exit 1
>> }
>> Write-Host "Registro OK. totemId=$($resp.totemId)  casino=$($resp.casino.nombre)" -ForegroundColor Green
>>
>> # 4) Escribir credenciales en SQLite local (writer dentro de C:\BuenaMezcla para que node resuelva el modulo)
>> Write-Host "=== 3) Guardando credenciales en SQLite ===" -ForegroundColor Yellow
>> $writerJs = "$Install\bm-writer.js"
>> $bs3Path = ($bs3Dir -replace '\\','/')
>> $dbPath  = "$($Install -replace '\\','/')/totem-data/totem.db"
>> @"
>> const Database = require('$bs3Path');
>> const db = new Database('$dbPath');
>> db.exec("CREATE TABLE IF NOT EXISTS totem_config (key TEXT PRIMARY KEY, value TEXT NOT NULL);");
>> const upsert = db.prepare('INSERT OR REPLACE INTO totem_config(key, value) VALUES(?, ?)');
>> const a = JSON.parse(process.argv[2]);
>> upsert.run('totem_id',      a.totemId);
>> upsert.run('totem_secret',  a.secret);
>> upsert.run('casino_id',     a.casinoId);
>> upsert.run('casino_nombre', a.casinoNombre);
>> upsert.run('cloud_url',     a.cloudUrl);
>> upsert.run('version',       '1.0.0');
>> console.log('OK credenciales guardadas');
>> "@ | Set-Content -Encoding ASCII -Path $writerJs
>>
>> $payload = @{
>>   totemId       = $resp.totemId
>>   secret        = $resp.secret
>>   casinoId      = $resp.casino.id
>>   casinoNombre  = $resp.casino.nombre
>>   cloudUrl      = $Cloud
>> } | ConvertTo-Json -Compress
>>
>> Push-Location $Install
>> & "$Install\node\node.exe" $writerJs $payload
>> $rc = $LASTEXITCODE
>> Pop-Location
>> Remove-Item $writerJs -Force -ErrorAction SilentlyContinue
>> if ($rc -ne 0) { Write-Host "FALLO al escribir SQLite" -ForegroundColor Red; Read-Host; exit 1 }
>>
>> # 5) run-totem.cmd
>> Write-Host "=== 4) Generando run-totem.cmd ===" -ForegroundColor Yellow
>> New-Item -ItemType Directory -Force -Path "$Install\scripts" | Out-Null
>> New-Item -ItemType Directory -Force -Path "$Install\logs"    | Out-Null
>> @"
>> @echo off
>> set DB_MODE=totem
>> set TOTEM_DB_PATH=$Install\totem-data\totem.db
>> set CLOUD_URL=$Cloud
>> set PORT=5000
>> set NODE_ENV=production
>> cd /d "$Install"
>> "$Install\node\node.exe" "$Install\totem\runtime.js" >> "$Install\logs\service.out.log" 2>> "$Install\logs\service.err.log"
>> "@ | Set-Content -Encoding ASCII -Path "$Install\scripts\run-totem.cmd"
>>
>> # 6) Tarea programada
>> Write-Host "=== 5) Tarea programada ===" -ForegroundColor Yellow
>> schtasks /Delete /TN "BuenaMezclaTotem" /F 2>$null | Out-Null
>> schtasks /Create /TN "BuenaMezclaTotem" /TR "`"$Install\scripts\run-totem.cmd`"" /SC ONSTART /RU "SYSTEM" /RL HIGHEST /F | Out-Null
>>
>> # 7) Arrancar
>> Write-Host "=== 6) Arrancando servicio ===" -ForegroundColor Yellow
>> schtasks /Run /TN "BuenaMezclaTotem"
>> Start-Sleep -Seconds 12
>>
>> # 8) Verificar
>> $running = Get-CimInstance Win32_Process -Filter "Name='node.exe'" -ErrorAction SilentlyContinue |
>>   Where-Object { $_.ExecutablePath -like "$Install\*" }
>> if ($running) { Write-Host "node.exe corriendo (PID $($running.ProcessId))" -ForegroundColor Green }
>> else {
>>   Write-Host "node.exe NO arranco. err.log:" -ForegroundColor Red
>>   if (Test-Path "$Install\logs\service.err.log") { Get-Content "$Install\logs\service.err.log" -Tail 25 }
>> }
>>
>> Start-Sleep -Seconds 35
>> Write-Host ""
>> Write-Host "=== Logs del sync (deben decir 'pull ok', NO '401') ===" -ForegroundColor Cyan
>> Get-Content "$Install\logs\service.out.log" -Tail 20
=== 1) Buscando better-sqlite3 ===
No esta instalado. Instalando con npm...
npm.cmd : npm warn deprecated prebuild-install@7.1.3: No longer maintained. Please contact the author of the relevant
native addon; alternatives are available.
En línea: 33 Carácter: 3
+   & "$Install\node\npm.cmd" install better-sqlite3@12.9.0 --omit=dev  ...
+   ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: (npm warn deprec... are available.:String) [], RemoteException
    + FullyQualifiedErrorId : NativeCommandError

PS C:\BuenaMezcla>
PS C:\BuenaMezcla> $ErrorActionPreference = "Continue"
>> $Install = "C:\BuenaMezcla"
>> $Cloud   = "https://vascan.replit.app"
>> $Token   = "g7Mqlz9HyQdlZqunnXcMCD6Qgv9PLGdc"
>> $Nombre  = "Totem-$($env:COMPUTERNAME)"
>>
>> # 0) Matar node viejo
>> Get-CimInstance Win32_Process -Filter "Name='node.exe'" -ErrorAction SilentlyContinue |
>>   Where-Object { $_.ExecutablePath -like "$Install\*" } |
>>   ForEach-Object { Stop-Process -Id $_.ProcessId -Force -ErrorAction SilentlyContinue }
>> Start-Sleep -Seconds 2
>>
>> # 1) Verificar/instalar better-sqlite3
>> Write-Host "=== 1) Verificando better-sqlite3 ===" -ForegroundColor Yellow
>> $bs3Dir = "$Install\node_modules\better-sqlite3"
>> if (-not (Test-Path "$bs3Dir\package.json")) {
>>   Write-Host "Instalando better-sqlite3 (warnings se ignoran)..." -ForegroundColor Yellow
>>   Push-Location $Install
>>   cmd /c "`"$Install\node\npm.cmd`" install better-sqlite3@12.9.0 --omit=dev --ignore-scripts --no-audit --no-fund 2>&1" | Out-Host
>>   $npmRc = $LASTEXITCODE
>>   Pop-Location
>>   if (-not (Test-Path "$bs3Dir\package.json")) {
>>     Write-Host "FALLO npm install (codigo $npmRc)" -ForegroundColor Red; Read-Host; exit 1
>>   }
>> }
>> $rel = "$bs3Dir\build\Release"
>> New-Item -ItemType Directory -Force -Path $rel | Out-Null
>> if (-not (Test-Path "$rel\better_sqlite3.node")) {
>>   Write-Host "Bajando binario nativo..." -ForegroundColor Yellow
>>   Invoke-WebRequest -Uri "$Cloud/totem/bs3-win.node" -OutFile "$rel\better_sqlite3.node" -UseBasicParsing
>> }
>> Write-Host "better-sqlite3 OK en $bs3Dir" -ForegroundColor Green
>>
>> # 2) IP local
>> $ipLocal = ""
>> try {
>>   $ipLocal = (Get-NetIPAddress -AddressFamily IPv4 -ErrorAction SilentlyContinue |
>>     Where-Object { $_.IPAddress -notlike "127.*" -and $_.IPAddress -notlike "169.*" } |
>>     Select-Object -First 1).IPAddress
>> } catch {}
>>
>> # 3) Registrar
>> Write-Host "=== 2) Registrando contra la nube ===" -ForegroundColor Yellow
>> $body = @{ nombre = $Nombre; hostname = $env:COMPUTERNAME; ipLocal = $ipLocal; version = "1.0.0" } | ConvertTo-Json -Compress
>> $resp = $null
>> try {
>>   $resp = Invoke-RestMethod -Uri "$Cloud/api/totem/register" -Method POST `
>>             -Headers @{ "x-bootstrap-token" = $Token; "content-type" = "application/json" } `
>>             -Body $body -TimeoutSec 15
>> } catch {
>>   Write-Host "Registro FALLO: $_" -ForegroundColor Red
>>   Write-Host "Si el token expiro, pedi uno nuevo." -ForegroundColor Red
>>   Read-Host; exit 1
>> }
>> Write-Host "Registro OK. totemId=$($resp.totemId)  casino=$($resp.casino.nombre)" -ForegroundColor Green
>>
>> # 4) Escribir credenciales
>> Write-Host "=== 3) Guardando credenciales en SQLite ===" -ForegroundColor Yellow
>> $writerJs = "$Install\bm-writer.js"
>> $bs3Path = ($bs3Dir -replace '\\','/')
>> $dbPath  = "$($Install -replace '\\','/')/totem-data/totem.db"
>> @"
>> const Database = require('$bs3Path');
>> const db = new Database('$dbPath');
>> db.exec("CREATE TABLE IF NOT EXISTS totem_config (key TEXT PRIMARY KEY, value TEXT NOT NULL);");
>> const upsert = db.prepare('INSERT OR REPLACE INTO totem_config(key, value) VALUES(?, ?)');
>> const a = JSON.parse(process.argv[2]);
>> upsert.run('totem_id',      a.totemId);
>> upsert.run('totem_secret',  a.secret);
>> upsert.run('casino_id',     a.casinoId);
>> upsert.run('casino_nombre', a.casinoNombre);
>> upsert.run('cloud_url',     a.cloudUrl);
>> upsert.run('version',       '1.0.0');
>> console.log('OK credenciales guardadas');
>> "@ | Set-Content -Encoding ASCII -Path $writerJs
>>
>> $payload = @{
>>   totemId       = $resp.totemId
>>   secret        = $resp.secret
>>   casinoId      = $resp.casino.id
>>   casinoNombre  = $resp.casino.nombre
>>   cloudUrl      = $Cloud
>> } | ConvertTo-Json -Compress
>>
>> Push-Location $Install
>> & "$Install\node\node.exe" $writerJs $payload
>> $rc = $LASTEXITCODE
>> Pop-Location
>> Remove-Item $writerJs -Force -ErrorAction SilentlyContinue
>> if ($rc -ne 0) { Write-Host "FALLO escribir SQLite (codigo $rc)" -ForegroundColor Red; Read-Host; exit 1 }
>>
>> # 5) run-totem.cmd
>> Write-Host "=== 4) Generando run-totem.cmd ===" -ForegroundColor Yellow                                              >> New-Item -ItemType Directory -Force -Path "$Install\scripts" | Out-Null                                              >> New-Item -ItemType Directory -Force -Path "$Install\logs"    | Out-Null                                              >> @"                                                                                                                   >> @echo off
>> set DB_MODE=totem
>> set TOTEM_DB_PATH=$Install\totem-data\totem.db
>> set CLOUD_URL=$Cloud
>> set PORT=5000
>> set NODE_ENV=production
>> cd /d "$Install"
>> "$Install\node\node.exe" "$Install\totem\runtime.js" >> "$Install\logs\service.out.log" 2>> "$Install\logs\service.err.log"
>> "@ | Set-Content -Encoding ASCII -Path "$Install\scripts\run-totem.cmd"
>>
>> # 6) Tarea programada
>> schtasks /Delete /TN "BuenaMezclaTotem" /F 2>$null | Out-Null
>> schtasks /Create /TN "BuenaMezclaTotem" /TR "`"$Install\scripts\run-totem.cmd`"" /SC ONSTART /RU "SYSTEM" /RL HIGHEST /F | Out-Null
>>
>> # 7) Arrancar
>> Write-Host "=== 5) Arrancando servicio ===" -ForegroundColor Yellow
>> schtasks /Run /TN "BuenaMezclaTotem"
>> Start-Sleep -Seconds 12
>>
>> # 8) Verificar
>> $running = Get-CimInstance Win32_Process -Filter "Name='node.exe'" -ErrorAction SilentlyContinue |
>>   Where-Object { $_.ExecutablePath -like "$Install\*" }
>> if ($running) { Write-Host "node.exe corriendo (PID $($running.ProcessId))" -ForegroundColor Green }
>> else {
>>   Write-Host "node.exe NO arranco. err.log:" -ForegroundColor Red
>>   if (Test-Path "$Install\logs\service.err.log") { Get-Content "$Install\logs\service.err.log" -Tail 25 }
>> }
>>
>> Start-Sleep -Seconds 35
>> Write-Host ""
>> Write-Host "=== Logs del sync (deben decir 'pull ok', NO '401') ===" -ForegroundColor Cyan
>> Get-Content "$Install\logs\service.out.log" -Tail 20
=== 1) Verificando better-sqlite3 ===
Bajando binario nativo...
better-sqlite3 OK en C:\BuenaMezcla\node_modules\better-sqlite3
=== 2) Registrando contra la nube ===
Registro OK. totemId=4312f164-bc6a-4990-b91f-403054a69252  casino=CASINO UNION QUIMICA
=== 3) Guardando credenciales en SQLite ===
undefined:1
{totemId:4312f164-bc6a-4990-b91f-403054a69252,casinoNombre:CASINO UNION QUIMICA,cloudUrl:https://vascan.replit.app,casinoId:4eba24b0-4612-40ef-b96a-02d0d6e69036,secret:7fxg72MjWPh3TYTwKd8mnufpUVAWJRwALPLN7gqg5HGhJnhm}
 ^

SyntaxError: Expected property name or '}' in JSON at position 1
    at JSON.parse (<anonymous>)
    at Object.<anonymous> (C:\BuenaMezcla\bm-writer.js:5:16)
    at Module._compile (node:internal/modules/cjs/loader:1469:14)
    at Module._extensions..js (node:internal/modules/cjs/loader:1548:10)
    at Module.load (node:internal/modules/cjs/loader:1288:32)
    at Module._load (node:internal/modules/cjs/loader:1104:12)
    at Function.executeUserEntryPoint [as runMain] (node:internal/modules/run_main:174:12)
    at node:internal/main/run_main_module:28:49

Node.js v20.18.0
FALLO escribir SQLite (codigo 1)







