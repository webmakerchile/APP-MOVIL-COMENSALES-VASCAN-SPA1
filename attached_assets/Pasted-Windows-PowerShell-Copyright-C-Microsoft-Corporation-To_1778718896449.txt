Windows PowerShell
Copyright (C) Microsoft Corporation. Todos los derechos reservados.

Prueba la nueva tecnología PowerShell multiplataforma https://aka.ms/pscore6

PS C:\Windows\system32> $serviceCmd = "$Install\scripts\run-totem.cmd"
>> New-Item -ItemType Directory -Force -Path "$Install\scripts" | Out-Null
>> @"
>> @echo off
>> set DB_MODE=totem
>> set TOTEM_DB_PATH=$Install\totem-data\totem.db
>> set CLOUD_URL=$Cloud
>> set PORT=5000
>> set NODE_ENV=production
>> cd /d "$Install"
>> "$Install\node\node.exe" "$Install\totem\runtime.js" >> "$Install\logs\service.out.log" 2>> "$Install\logs\service.err.log"
>> "@ | Set-Content -Encoding ASCII $serviceCmd
PS C:\Windows\system32> $serviceCmd = "$Install\scripts\run-totem.cmd"
>> New-Item -ItemType Directory -Force -Path "$Install\scripts" | Out-Null
>> @"
>> @echo off
>> set DB_MODE=totem
>> set TOTEM_DB_PATH=$Install\totem-data\totem.db
>> set CLOUD_URL=$Cloud
>> set PORT=5000
>> set NODE_ENV=production
>> cd /d "$Install"
>> "$Install\node\node.exe" "$Install\totem\runtime.js" >> "$Install\logs\service.out.log" 2>> "$Install\logs\service.err.log"
>> "@ | Set-Content -Encoding ASCII $serviceCmd
>> Write-Host "=== Recreando tarea programada ===" -ForegroundColor Yellow
>> schtasks /Delete /TN "BuenaMezclaTotem" /F 2>$null | Out-Null
>> schtasks /Create /TN "BuenaMezclaTotem" /TR "`"$serviceCmd`"" /SC ONSTART /RU "SYSTEM" /RL HIGHEST /F | Out-Null
=== Recreando tarea programada ===
PS C:\Windows\system32> schtasks /Run /TN "BuenaMezclaTotem"
>> Start-Sleep -Seconds 10
CORRECTO: se ha intentado ejecutar la tarea programada "BuenaMezclaTotem".
PS C:\Windows\system32> Write-Host ""
>> Write-Host "=== Verificacion ===" -ForegroundColor Yellow
>> $running = Get-CimInstance Win32_Process -Filter "Name='node.exe'" -ErrorAction SilentlyContinue |
>>   Where-Object { $_.ExecutablePath -like "$Install\*" }
>> if ($running) { Write-Host "node.exe corriendo (PID $($running.ProcessId))" -ForegroundColor Green }
>> else          { Write-Host "node.exe NO arranco" -ForegroundColor Red }
>> try {
>>   Invoke-WebRequest -UseBasicParsing -TimeoutSec 3 http://127.0.0.1:5000/api/auth/me | Out-Null
>>   Write-Host "Express local OK" -ForegroundColor Green
>> } catch { Write-Host "Express local NO responde" -ForegroundColor Red }
>> Write-Host ""
>> Write-Host "Esperá ~40 segundos y mirá los logs:" -ForegroundColor Cyan
>> Write-Host "  Get-Content C:\BuenaMezcla\logs\service.out.log -Tail 15" -ForegroundColor Cyan
>> Write-Host "Tiene que aparecer '[sync] pull ok' (sin 401)." -ForegroundColor Cyan
>> Write-Host "Despues, login en el totem: 12345678-5 / 123456" -ForegroundColor Cyan

=== Verificacion ===
node.exe NO arranco
Express local NO responde

Esperá ~40 segundos y mirá los logs:
  Get-Content C:\BuenaMezcla\logs\service.out.log -Tail 15
Tiene que aparecer '[sync] pull ok' (sin 401).
Despues, login en el totem: 12345678-5 / 123456
PS C:\Windows\system32>